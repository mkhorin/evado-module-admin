<%
layout(_view.get(_layout));
block('tab', 'User password');
%>

<%- include(_view.get('_part/attr/string'), {
    attr: 'password',
    placeholder: model.isNew() ? '' : _t('Input new password to replace the current one')
}) %>